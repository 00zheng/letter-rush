# Supabase RPC contract

This table records the deployed-client contract for the services repaired on
2026-07-25. Every listed function is `SECURITY DEFINER`, uses
`set search_path = ''`, revokes execution from `PUBLIC` and `anon`, and grants
execution only to `authenticated`.

| Client RPC                      | Defining/latest migration                                                     | SQL arguments                                                      | SQL return type                                                                                                                                                                                                                                     | Grant                   | Caller                               |
| ------------------------------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | ------------------------------------ |
| `get_current_player_challenges` | `20260725004120_custom_match_quality_challenges_and_word_opportunities.sql`   | none                                                               | `table(challenge_id uuid, direction text, opponent_public_profile_id text, opponent_display_name text, rated boolean, challenge_status text, expires_at timestamptz, match_id uuid, match_mode match_mode, room_code text, server_now timestamptz)` | `authenticated EXECUTE` | `use-player-challenges`              |
| `create_player_challenge`       | `20260725063941_repair_challenges_rematches_preview_votes_and_solver.sql`     | `p_public_profile_id text`, `p_rated boolean`                      | same challenge projection as `get_current_player_challenges`                                                                                                                                                                                        | `authenticated EXECUTE` | `profile-challenge-controls`         |
| `respond_player_challenge`      | `20260725063941_repair_challenges_rematches_preview_votes_and_solver.sql`     | `p_challenge_id uuid`, `p_accept boolean`                          | `table(challenge_status text, match_id uuid, match_mode match_mode, room_code text, server_now timestamptz)`                                                                                                                                        | `authenticated EXECUTE` | challenge inbox and profile controls |
| `cancel_player_challenge`       | `20260725004120_custom_match_quality_challenges_and_word_opportunities.sql`   | `p_challenge_id uuid`                                              | `boolean`                                                                                                                                                                                                                                           | `authenticated EXECUTE` | challenge inbox and profile controls |
| `get_two_player_rematch_state`  | `20260725000442_authoritative_match_lifecycle_rematches_and_board_limits.sql` | `p_match_id uuid`                                                  | `table(proposal_id uuid, proposal_status rematch_proposal_status, requested_by_me boolean, can_respond boolean, expires_at timestamptz, created_match_id uuid, server_now timestamptz)`                                                             | `authenticated EXECUTE` | `rematch-controls`                   |
| `request_two_player_rematch`    | `20260725000442_authoritative_match_lifecycle_rematches_and_board_limits.sql` | `p_match_id uuid`                                                  | same rematch state as `get_two_player_rematch_state`                                                                                                                                                                                                | `authenticated EXECUTE` | `rematch-controls`                   |
| `respond_two_player_rematch`    | `20260725063941_repair_challenges_rematches_preview_votes_and_solver.sql`     | `p_proposal_id uuid`, `p_accept boolean`                           | `table(proposal_status rematch_proposal_status, match_id uuid, expires_at timestamptz, server_now timestamptz)`                                                                                                                                     | `authenticated EXECUTE` | `rematch-controls`                   |
| `cancel_two_player_rematch`     | `20260725000442_authoritative_match_lifecycle_rematches_and_board_limits.sql` | `p_proposal_id uuid`                                               | `table(proposal_status rematch_proposal_status, server_now timestamptz)`                                                                                                                                                                            | `authenticated EXECUTE` | `rematch-controls`                   |
| `get_match_preview_state`       | `20260725063941_repair_challenges_rematches_preview_votes_and_solver.sql`     | `p_match_id uuid`                                                  | atomic `table(board_seed bigint, board_revision integer, reroll and skip vote state, preview timestamps, match_status match_status, server_now timestamptz)`                                                                                        | `authenticated EXECUTE` | `pregame-preview`                    |
| `vote_match_reroll_cycle`       | `20260725063941_repair_challenges_rematches_preview_votes_and_solver.sql`     | `p_match_id uuid`, `p_board_revision integer`, `p_approve boolean` | same atomic snapshot as `get_match_preview_state`                                                                                                                                                                                                   | `authenticated EXECUTE` | `pregame-preview`                    |
| `vote_match_reroll`             | `20260725063941_repair_challenges_rematches_preview_votes_and_solver.sql`     | `p_match_id uuid`, `p_approve boolean`                             | `table(reroll_used boolean, approvals integer, declines integer, participant_count integer, board_seed bigint, preview_ends_at timestamptz, server_now timestamptz)`                                                                                | `authenticated EXECUTE` | older compatible clients             |
| `vote_match_countdown_skip`     | `20260725063941_repair_challenges_rematches_preview_votes_and_solver.sql`     | `p_match_id uuid`, `p_board_revision integer`                      | same atomic snapshot as `get_match_preview_state`                                                                                                                                                                                                   | `authenticated EXECUTE` | `pregame-preview`                    |
| `get_match_word_opportunities`  | `20260725063941_repair_challenges_rematches_preview_votes_and_solver.sql`     | `p_match_id uuid`                                                  | `table(word text, word_length integer, score integer, recognizable boolean, was_found boolean)`                                                                                                                                                     | `authenticated EXECUTE` | result analysis hook                 |

`scripts/check-rpc-contract.mjs` is part of `prebuild`; a client RPC name that is
absent from either version-controlled migrations or the generated TypeScript
contract fails the build before Next.js compilation.
